# Atlantis Collaboration

Extensión Code-OSS/VSCodium para el hub Atlantis. Los dos paneles del espacio
compartido muestran canales `Equipo` y `Agentes`; los workers Hermes siguen
siendo procesos locales de cada desarrollador.

```bash
npm install
npm run check
npm run build
npm run package
```
